{
  "name": "todo-backend",
  "version": "0.0.0",
  "scripts": {
    "start": "nodemon ./www.js localhost 3080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
